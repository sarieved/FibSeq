# Home page
GET     /                           controllers.FibController.index

# Widgets
GET     /sequence                    controllers.FibController.list
POST    /sequence                    controllers.FibController.createSeq

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
